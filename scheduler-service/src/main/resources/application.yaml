server:
  port: 8085
  servlet:
    context-path: /scheduler-service

spring:
  application:
    name: scheduler-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true